Minimal Flutter sample for CI demonstration

How to run locally

- Install Flutter: https://flutter.dev/docs/get-started/install
- Run: `flutter pub get` then `flutter test`

Purpose
- Provide a small Flutter package that can be built and tested in CI and serve as a template for adding more Flutter samples.